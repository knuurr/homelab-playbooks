" Set Vim in non-compatible mode
set nocompatible

" Enable file type detection, plugin support, and auto-indentation
filetype plugin indent on

" Set the title of the Vim window to the current file name
set title

" Enable syntax highlighting
syntax on

" Highlight search results
set hlsearch

" Show the ruler with the current cursor position
set ruler

" Display line numbers
set number

" Enable mouse support
set mouse=a

" Enable auto-indentation
set autoindent

" Set the number of spaces for a soft tab and indentation
set softtabstop=2
set tabstop=2
set shiftwidth=2

" Expand tabs to spaces
set expandtab

" Disable error bells
set noerrorbells

" Disable visual bells
set novisualbell